services:
  app:
    build:
      context: .
      target: production
    ports:
      - "127.0.0.1:8081:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    env_file:
      - .env
      - path: .env.local
        required: false