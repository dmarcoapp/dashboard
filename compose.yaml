services:
  app:
    build:
      context: .
      target: development
    ports:
      - "127.0.0.1:5173:8080"
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env
      - path: .env.local
        required: false
